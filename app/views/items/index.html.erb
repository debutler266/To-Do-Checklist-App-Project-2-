<% if user_signed_in? %>
  <% @items.each do |item| %>
  <h2><%= link_to item.title, item %></h2>
  <p><%= item.description %></p>
  <%= link_to "Mark as Completed", complete_item_path(item), method: :patch %>
<% end %>
<% else %>
<%= render 'layouts/static_landing_page' %>
<% end %>
